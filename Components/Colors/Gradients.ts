import { Colors } from './Colors'

export const Gradients = {
  LightGreen: `bg-gradient-to-r from-${Colors.LightGreen} to-${Colors.OpaqueYellow}`,
  LightBlue: `bg-gradient-to-r from-${Colors.LightBlue} to-${Colors.OpaqueYellow}`,
  LightYellow: `bg-gradient-to-r from-${Colors.LightYellow} to-${Colors.OpaqueYellow}`,
  LightPurple: `bg-gradient-to-r from-${Colors.LightPurple} to-${Colors.OpaqueYellow}`,
  LightGray: `bg-gradient-to-r from-${Colors.LightGray} to-${Colors.OpaqueYellow}`,
}